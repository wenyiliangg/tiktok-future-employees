# Paired delta artifacts

This directory stores deterministic machine-readable paired comparisons between
each official candidate and the current campaign champion. Raw evaluator traces
remain ignored; committed artifacts contain aggregate and session-identifier
diagnostics only.
